# PG ACR Check-IN
ระบบลงทะเบียนและติดตามการเข้าใช้ศูนย์การเรียนรู้ของโรงเรียนอัสสัมชัญระยอง พัฒนาด้วย Next.js 16, React 19 และชุด UI แบบ Tailwind เพื่อให้ทั้งนักเรียนและผู้ดูแลห้องสมุดทำงานได้รวดเร็ว มีการตรวจสอบตัวตน, บันทึกการเข้าใช้, วิเคราะห์สถิติ และรองรับการนำเข้ารายชื่อนักเรียนจากไฟล์ JSON

> โปรเจกต์นี้มี 2 รูปแบบให้เลือกใช้งาน ได้แก่ **MYSQL-VERSION** (เชื่อม MySQL โดยตรง) และ **SHEET-VERSION** (อ่าน/เขียนข้อมูลผ่าน Google Sheets สำหรับทีมที่ไม่ต้องการดูแลฐานข้อมูลเอง)

**พัฒนาโดย Akarapach Yootsukprasert (Park AKA PG Dev.) นักเรียนชั้น ม.6 โรงเรียนอัสสัมชัญระยอง ปีการศึกษา 2025**

## สารบัญ
1. [ภาพรวมโปรเจกต์](#ภาพรวมโปรเจกต์)
2. [รูปแบบการใช้งาน](#รูปแบบการใช้งาน)
3. [คุณสมบัติเด่น](#คุณสมบัติเด่น)
4. [สถาปัตยกรรมและโครงสร้างไฟล์](#สถาปัตยกรรมและโครงสร้างไฟล์)
5. [เทคโนโลยีหลัก](#เทคโนโลยีหลัก)
6. [การเตรียมสภาพแวดล้อม](#การเตรียมสภาพแวดล้อม)
7. [วิธีติดตั้งและรันโปรเจกต์](#วิธีติดตั้งและรันโปรเจกต์)
8. [การใช้งาน Docker](#การใช้งาน-docker)
9. [การตั้งค่า MYSQL-VERSION](#การตั้งค่า-mysql-version)
10. [การตั้งค่า SHEET-VERSION](#การตั้งค่า-sheet-version)
11. [สคริปต์ที่ใช้บ่อย](#สคริปต์ที่ใช้บ่อย)
12. [การใช้งานระบบ](#การใช้งานระบบ)
13. [การนำเข้ารายชื่อจากไฟล์ JSON](#การนำเข้ารายชื่อจากไฟล์-json)
14. [การป้องกันรหัสซ้ำและความน่าเชื่อถือของข้อมูล](#การป้องกันรหัสซ้ำและความน่าเชื่อถือของข้อมูล)
15. [การปรับแต่งเพิ่มเติม](#การปรับแต่งเพิ่มเติม)
16. [แนวทาง Deploy](#แนวทาง-deploy)

## ภาพรวมโปรเจกต์
- แยกส่วนหน้า (Student Portal) และหลังบ้าน (Admin Dashboard) ชัดเจนภายในหน้า Next.js เดียว
- ฟรอนต์เอนด์เป็น Single Page Experience ใช้งานได้ทั้งบนจอสัมผัสและเดสก์ท็อป
- ระบบผู้ดูแลรองรับการสืบค้น, กรองตามชั้น/ห้อง, ดูสถิติการเข้าใช้, ดาวน์โหลด CSV, ลบข้อมูลเฉพาะรายการ, รวมถึงการนำเข้ารายชื่อนักเรียนจำนวนมาก
- รองรับภาษาไทยเต็มรูปแบบ (Collation และ Sorting ใช้ `Intl.Collator("th-TH")`)

## รูปแบบการใช้งาน
| รายการ | MYSQL-VERSION | SHEET-VERSION |
| --- | --- | --- |
| แหล่งข้อมูล | MySQL จริง (สร้าง schema อัตโนมัติ) | Google Sheets 2 ชุด (Students & Attendance) |
| ความเหมาะสม | ใช้ในระบบ production ที่มีเซิร์ฟเวอร์ถาวร | ใช้งานเร็ว ไม่ต้องดูแล DB และแบ่งปันข้อมูลได้ทันที |
| ความสามารถ | INSERT/UPDATE ด้วย Transaction DB, JOIN ตรง | อ่าน/เขียนผ่าน Google API, แชร์กับผู้ใช้ที่ไม่มีสิทธิ์ DB |
| การป้องกันรหัสซ้ำ | ใช้ `AUTO_INCREMENT` + `UNIQUE student_code` | สร้างรหัสด้วย `generateRecordId` แล้วตรวจซ้ำก่อนบันทึก |

## คุณสมบัติเด่น
- 🔐 **การยืนยันตัวตนผู้ดูแล** ผ่านรหัสผ่านและโทเคนเซสชันที่ตั้งใน `.env`
- 🧑‍🎓 **นักเรียนล็อกอินด้วยเลขประจำตัว** ตรวจสอบได้ทันทีจากฐานข้อมูล/ชีต
- 📋 **รายการจุดประสงค์ (Purposes) ปรับแต่งได้** ใน `lib/purposes-data.ts`
- 📈 **แดชบอร์ดสถิติ** แสดงจำนวนครั้งเข้าใช้, หัวข้อยอดนิยม, จัดกลุ่มตามชั้น/ห้อง
- ⬆️ **นำเข้าข้อมูลนักเรียนแบบ JSON** รองรับไฟล์จากระบบภายใน, JSON Lines และไฟล์ที่มี BOM
- 🧾 **ส่งออก CSV** ได้ทั้งภาพรวมและแยกตามชั้น
- ⚖️ **ป้องกันข้อมูลซ้ำ** ทั้งระดับรหัสนักเรียนและบันทึกการเข้าห้องสมุดภายในวันเดียวกัน
- 🌐 **พร้อม Deploy** บน Vercel/Node server ด้วย Next.js App Router

## สถาปัตยกรรมและโครงสร้างไฟล์
โปรเจกต์อยู่ในไดเรกทอรีหลักเดียวกันและแยกเวอร์ชันไว้ในโฟลเดอร์ย่อย

```
.
├── MYSQL-VERSION      # เวอร์ชันเชื่อม MySQL
│   ├── app            # หน้า Next.js + API Routes
│   ├── components     # คอมโพเนนต์ UI/แดชบอร์ด
│   └── lib            # DB connector, service layer
└── SHEET-VERSION      # เวอร์ชันเชื่อม Google Sheets
    ├── app
    ├── components
    └── lib            # Google Sheets client, services
```

ไฟล์อื่นๆ ที่เหมือนกันระหว่างสองเวอร์ชัน ได้แก่ `package.json`, `pnpm-lock.yaml`, `postcss.config.mjs`, `tsconfig.json`, และทรัพยากรภาพใน `public/`

## เทคโนโลยีหลัก
- [Next.js 16 (App Router)](https://nextjs.org/) + [React 19](https://react.dev/)
- [TypeScript 5](https://www.typescriptlang.org/)
- [Tailwind CSS 4](https://tailwindcss.com/) + shadcn/ui components
- [mysql2](https://github.com/sidorares/node-mysql2) (เฉพาะ MYSQL-VERSION)
- [googleapis Sheets API v4](https://github.com/googleapis/google-api-nodejs-client) (เฉพาะ SHEET-VERSION)
- [Vercel Analytics](https://vercel.com/docs/analytics) (พร้อมใช้งาน)

## การเตรียมสภาพแวดล้อม
1. ติดตั้ง Node.js 20 ขึ้นไป (เวอร์ชันที่ Next.js 16 รองรับ)
2. แนะนำให้ใช้ [pnpm](https://pnpm.io/) 9+ หรือ Bun 1.1+ เพื่อให้อ้างอิง `pnpm-lock.yaml`/`bun.lock` ได้ตรง
3. ติดตั้ง MySQL Server (กรณีใช้ MYSQL-VERSION)
4. สร้าง Service Account บน Google Cloud พร้อมสิทธิ์อ่าน/เขียน Sheets (กรณีใช้ SHEET-VERSION)

## วิธีติดตั้งและรันโปรเจกต์
```bash
git clone https://github.com/PGTHEGOD/PG-ACR-Check-IN.git
cd PG-ACR-Check-IN

# เลือกเวอร์ชันที่ต้องการ
cd MYSQL-VERSION          # หรือ cd SHEET-VERSION

pnpm install              # ติดตั้ง dependencies
pnpm dev                  # เปิดโหมดพัฒนา
```

> หากใช้ Bun สามารถเรียก `bun install` และ `bun dev` ได้เช่นกัน (สคริปต์ใน `package.json` ออกแบบให้ทำงานเหมือนกัน)

## การใช้งาน Docker
`Dockerfile` ที่รากโปรเจกต์ฝัง MariaDB สำหรับเวอร์ชัน MySQL (ค่าเริ่มต้น `APP_VARIANT=MYSQL-VERSION`) และยังรองรับการสลับไปใช้เวอร์ชัน Google Sheets ผ่าน build arg. คุณสามารถตั้งค่า Node ได้ด้วย `NODE_VERSION`

```bash
# สร้างอิมเมจ (ค่าเริ่มต้นคือ MySQL พร้อม MariaDB ในคอนเทนเนอร์)
docker build \
  --build-arg APP_VARIANT=MYSQL-VERSION \
  -t acr-mysql .

# หรือสร้างสำหรับเวอร์ชัน Google Sheets
docker build \
  --build-arg APP_VARIANT=SHEET-VERSION \
  -t acr-sheets .

# รันคอนเทนเนอร์ พร้อมกำหนดไฟล์ .env ของเวอร์ชันที่เลือก
docker run --env-file MYSQL-VERSION/.env.local -p 3000:3000 acr-mysql
```

> หมายเหตุ
> - คอนเทนเนอร์สำหรับ MySQL จะเริ่ม MariaDB ภายในอัตโนมัติและสร้างฐานข้อมูล/ผู้ใช้ตามค่าในตัวแปร `MYSQL_DATABASE`, `MYSQL_USER`, `MYSQL_PASSWORD` (จำเป็นต้องระบุเอง)
> - อย่าลืมสร้างไฟล์ `.env.local` ให้พร้อมก่อนสั่ง build/run (สามารถสร้างไฟล์เฉพาะ Docker เช่น `.env.docker`)
> - หากต้องการรันในโหมด production จริง อาจเชื่อมต่อ volume สำหรับ `/var/lib/mysql` และปรับสิทธิ์/รหัสผ่านตามมาตรฐานของคุณ

## การตั้งค่า MYSQL-VERSION
### ไฟล์ `.env`
สร้าง `.env.local` ภายใต้ `MYSQL-VERSION/` แล้วตั้งค่า:

| ตัวแปร | คำอธิบาย |
| --- | --- |
| `MYSQL_HOST` `MYSQL_PORT` | ข้อมูลการเชื่อม MySQL |
| `MYSQL_DATABASE` | ชื่อฐานข้อมูล (จะสร้างให้อัตโนมัติถ้าไม่มี) |
| `MYSQL_USER` `MYSQL_PASSWORD` | สิทธิ์ที่อนุญาตให้สร้างตาราง/อ่าน/เขียน |
| `ADMIN_PASSWORD` | รหัสผ่านผู้ดูแลระบบ (ฝั่ง UI) |
| `ADMIN_SESSION_TOKEN` | ชื่อเซสชันคุกกี้ที่ใช้ผูกการเข้าสู่ระบบ |

### โครงสร้างฐานข้อมูล
ไฟล์ `lib/db.ts` จะตรวจสอบและสร้าง schema ให้โดยอัตโนมัติเมื่อแอปเริ่มต้น:
- ตาราง `students` มี `UNIQUE student_code` เพื่อกันข้อมูลซ้ำ และบันทึกชื่อ, ชั้น, ห้อง, เลขที่, คำนำหน้า
- ตาราง `attendance_logs` เชื่อม `student_id`, จำกัดให้ 1 แถวต่อวัน (`UNIQUE student_id + attendance_date`) เพื่อไม่ให้ล็อกอินซ้ำเวลาเดียวกัน

## การตั้งค่า SHEET-VERSION
### ไฟล์ `.env`
สร้าง `.env.local` ภายใต้ `SHEET-VERSION/` พร้อมค่าดังนี้

| ตัวแปร | คำอธิบาย |
| --- | --- |
| `ADMIN_PASSWORD`, `ADMIN_SESSION_TOKEN` | รหัสผ่านและชื่อคุกกี้ผู้ดูแล |
| `LIBRARY_TIMEZONE` | ไทม์โซนสำหรับแสดงวันที่ (ค่าเริ่มต้น `Asia/Bangkok`) |
| `GOOGLE_SHEETS_SPREADSHEET_ID` | ID ของ Spreadsheet |
| `GOOGLE_SHEETS_STUDENTS_SHEET` / `GOOGLE_SHEETS_ATTENDANCE_SHEET` | ชื่อชีตรายชื่อนักเรียนและการเข้าใช้ |
| `GOOGLE_SERVICE_ACCOUNT_EMAIL` / `GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` | ข้อมูล Service Account ที่มีสิทธิ์แก้ไขชีต |

### ขั้นตอนสำคัญ
1. สร้าง Spreadsheet ที่มีสองชีต (เช่น `Students` และ `Attendance`)
2. แชร์ให้ Service Account ที่สร้างไว้ด้วยสิทธิ์ Editor
3. คัดลอก ID/ชื่อชีตมาใส่ใน `.env`
4. ระบบ `lib/google-sheets.ts` จะสร้างหัวคอลัมน์และเตรียมโครงสร้างให้อัตโนมัติ

## สคริปต์ที่ใช้บ่อย
| คำสั่ง | คำอธิบาย |
| --- | --- |
| `pnpm dev` | รันโหมดพัฒนา (Next.js dev server) |
| `pnpm build` | คอมไพล์สำหรับ Production |
| `pnpm start` | เปิด Production server (ต้องรัน `pnpm build` ก่อน) |
| `pnpm lint` | ตรวจสอบโค้ดมาตรฐาน ESLint |

## การใช้งานระบบ
### ฝั่งนักเรียน
1. กรอกเลขประจำตัวบนหน้าแรก (`StudentLogin`)
2. หากพบข้อมูล ระบบจะพาไปหน้าบันทึก (`StudentRegister`) ให้เลือกจุดประสงค์
3. เมื่อเลือกและยืนยัน ระบบจะอัปเดตเวลาล่าสุดของวันนั้นและแสดงการบันทึกสำเร็จ

### ฝั่งผู้ดูแล
1. กดปุ่ม “ผู้ดูแลระบบ” แล้วกรอกรหัสผ่านที่ตั้งไว้ใน `.env`
2. ภายในแดชบอร์ดสามารถ:
   - กรองการเข้าใช้ตามเดือน, คำค้นหา
   - ดูสถิติรวมและอันดับจุดประสงค์
   - ส่งออก CSV ทั้งแบบสรุปและแยกรายชั้น
   - ลบรายการเฉพาะได้
   - นำเข้ารายชื่อนักเรียนด้วย JSON / JSONLines
   - กรองรายชื่อด้วยชั้น/ห้อง และค้นหาด้วยคีย์เวิร์ด

## การนำเข้ารายชื่อจากไฟล์ JSON
- รองรับโครงสร้างแบบ `Worksheet` (คล้ายที่ได้จาก Excel → JSON) และสามารถใช้คีย์ภาษาไทย เช่น `รหัสประจำตัว`, `ชั้น`, `ห้อง`, `เลขที่`, `คำนำหน้า`, `ชื่อ`, `นามสกุล`
- โค้ดรองรับไฟล์ที่มี BOM, มีบรรทัดว่าง หรือ JSON ที่ต้องซ่อมรูปแบบด้วย `jsonrepair`
- หากในไฟล์ไม่มีเลขประจำตัว ระบบจะขึ้นเตือนก่อนนำเข้า
- ระบบจะตรวจว่ามี “รหัสนักเรียนในระบบที่ไม่มีในไฟล์” หรือไม่:
  - ให้เลือก `--action=update` เพื่อข้ามการลบ
  - หรือ `--action=delete` เพื่อให้ระบบลบรายชื่อที่หายไปจากไฟล์ออกจากระบบด้วย

ตัวอย่าง payload:

```json
{
  "Worksheet": [
    {
      "รหัสประจำตัว": "24732",
      "ชั้น": "ป.6",
      "ห้อง": "1",
      "เลขที่": "12",
      "คำนำหน้า": "เด็กชาย",
      "ชื่อ": "คงคณิน",
      "นามสกุล": "สุขเกิด"
    }
  ]
}
```

## การป้องกันรหัสซ้ำและความน่าเชื่อถือของข้อมูล
- **MYSQL-VERSION** ใช้ข้อจำกัด `UNIQUE` ในฐานข้อมูล และ `attendance_logs` มีคีย์ผสม `(student_id, attendance_date)` เพื่อกันการลงทะเบียนซ้ำในวันเดียวกัน
- **SHEET-VERSION** ใช้ฟังก์ชัน `generateRecordId` ที่สุ่มตาม timestamp แล้วตรวจสอบกับ `Set` ของรหัสที่มีอยู่ก่อน เพื่อไม่ให้ชนกันระหว่างหลายผู้ใช้
- การลบรายชื่อนักเรียนใน SHEET-VERSION จะไล่ลบข้อมูลการเข้าห้องสมุดที่เชื่อมโยง เพื่อป้องกันข้อมูลกำพร้า

## การปรับแต่งเพิ่มเติม
- ปรับรายการจุดประสงค์ได้ที่ `lib/purposes-data.ts`
- สามารถแก้สไตล์/ฟอนต์ที่ `app/globals.css` และคอมโพเนนต์ใน `components/ui/`
- เปลี่ยนโลโก้และรูปภาพใน `public/`
- หากต้องการเพิ่มจุดประสงค์หรือข้อมูลเสริม ให้ขยาย `AttendanceRecord` และปรับคอลัมน์ในชีต/ฐานข้อมูลตามต้องการ

## แนวทาง Deploy
1. ตั้งค่า `.env` ของเวอร์ชันที่ต้องการในระบบ CI/CD หรือ Vercel Project
2. รัน `pnpm install && pnpm build`
3. ใช้ `pnpm start` บน Node server หรือให้ Vercel สร้างเป็น Serverless/Edge ตามค่าเริ่มต้น
4. สำหรับ MYSQL-VERSION ตรวจสอบให้แอปรันใน network เดียวกับฐานข้อมูลและเปิดพอร์ต 3306 ให้ปลอดภัย
5. สำหรับ SHEET-VERSION ให้ใส่ค่า Service Account ใน Secret Manager หรือ Environment Variables ของแพลตฟอร์ม

---
พัฒนาโดย Akarapach Yootsukprasert (Park AKA PG Dev.) นักเรียนชั้น ม.6 โรงเรียนอัสสัมชัญระยอง ปีการศึกษา 2025 – หากมีข้อเสนอแนะหรือพบปัญหา สามารถเปิด Issue ใน GitHub Repository `PGTHEGOD/PG-ACR-Check-IN`
